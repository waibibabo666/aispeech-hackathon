# 开发日志

> 项目：日知 (Rizhi)
> 黑客松：48小时
> 平台：Windows Desktop (pywebview)

---

## 2026-07-14 开发启动

### 项目初始化

- 创建项目仓库，初始化 Git
- 编写架构设计文档 `多模态识别任务管理器.md`
- 完成深度调研，输出 `调研结果.md`
- 确定技术路线：Python FastAPI 后端 + React 前端 + 云端 API

### 技术选型确认

| 组件 | 选型 | 理由 |
|------|------|------|
| 后端框架 | FastAPI | 异步支持好，AI 工具链自然语言 |
| LLM API | OpenAI SDK | 兼容公司 API 代理 |
| ASR | Whisper API | 一个 HTTP 请求，无需本地模型 |
| OCR | GPT-4o Vision | 直接传图片，无需独立 OCR 服务 |
| 前端 | React + TypeScript + Vite | 开发快，生态成熟 |
| 日历 | FullCalendar v6 | 周/月/列表三视图，中文支持 |
| 样式 | Tailwind CSS v4 | Vite 插件，零配置 |

### 后端搭建

- FastAPI 项目骨架（`main.py`、`config.py`）
- 数据模型：`Task`、`ExtractionResult`、`UploadResponse`
- 文件调度器 `Dispatcher` — 按后缀分发给对应解析器
- 5 个解析器：
  - `TextParser` — `.txt`/`.md`/`.csv` 直接读取
  - `DocxParser` — `python-docx` 提取段落
  - `PdfParser` — `PyMuPDF` 逐页提取
  - `ImageParser` — GPT-4o Vision API OCR
  - `AudioParser` — Whisper API 语音转文字
- LLM 提取层：中文 Prompt 工程 + JSON Schema Structured Outputs
- 置信度路由：≥80% 自动添加，50-79% 待确认，<50% 丢弃
- 内存任务存储：CRUD + Demo 数据加载

### 前端搭建

- React + TypeScript + Vite 项目初始化
- 组件树：`Layout → InputPanel → (TextInput | FileUpload) + TaskTimeline`
- FullCalendar 三视图：周视图 / 月视图 / 列表视图
- 拖拽文件上传（react-dropzone）
- 待确认任务浮动按钮 + 确认弹窗（PendingBadge）
- Vite 代理配置 `/api → localhost:8000`

### Demo 数据

7 条覆盖一周的日程任务：会议、健身、医疗、社交、截止日期、户外活动

---

## 2026-07-14 集成调试

### Bug 修复记录

#### 1. PyMuPDF 1.24.0 编译失败

**现象：** `pip install pymupdf==1.24.0` 报错 `Unable to find Visual Studio`

**原因：** 1.24.0 在 Windows Python 3.13 上没有预编译 wheel，需要本地编译 C 扩展，而本机未安装 Visual Studio

**修复：** 升级到 `pymupdf>=1.28.0`，该版本提供了 `cp310-abi3-win_amd64` 预编译 wheel

**耗时：** 10 分钟

---

#### 2. image_parser / audio_parser import 路径错误

**现象：**
```
ModuleNotFoundError: No module named 'backend.services.config'
```

**原因：** `image_parser.py` 和 `audio_parser.py` 使用 `from ..config import settings`，但由于它们在 `services/parsers/` 目录下（深一层），需要三个点 `...` 才能回到 `backend` 包的根

**修复：** `from ..config import settings` → `from ...config import settings`

**耗时：** 2 分钟

---

#### 3. useTasks.ts JSX 语法错误

**现象：** Vite 编译报错 `Expected ">" but found "value"` 在 `useTasks.ts:150`

**原因：** 文件包含 JSX（`<TaskContext.Provider value={...}>`），但扩展名是 `.ts`，esbuild 不会用 JSX 模式解析 `.ts` 文件

**修复：** 重命名为 `useTasks.tsx`

**耗时：** 2 分钟

---

#### 4. FullCalendar 视图切换无效

**现象：** 点击「周」「月」「列表」按钮，视图不切换

**原因：** FullCalendar 的 `initialView` prop 仅在首次挂载时生效，后续 state 变化不会触发视图更新

**修复：** 给 `<FullCalendar>` 组件添加 `key={view}`，每次视图变化时强制 React 重新挂载组件

**耗时：** 2 分钟

---

#### 5. httpx 版本冲突

**现象：** 
```
AsyncClient.__init__() got an unexpected keyword argument 'proxies'
```

**原因：** `httpx 0.28.x` 移除了 `proxies` 参数，而 `openai 1.55.0` 仍然传递该参数

**修复：** 升级 `openai` 到 `2.45.0`（最新版本已适配新版 httpx）

**耗时：** 5 分钟

---

#### 6. 模型兼容性

**现象：** `.env` 中 `MODEL_NAME=gpt-image-2`，调用 chat completions 报错：
```
The 'gpt-image-2' model is not supported when using Codex with a ChatGPT account
```

**原因：** `gpt-image-2` 是图像生成模型，不支持 chat completions

**修复：** 改为 `gpt-5.5`（公司 API 代理支持的文本模型）

**耗时：** 5 分钟

---

### 集成测试结果

| 测试项 | 输入 | 期望 | 实际 | 状态 |
|--------|------|------|------|------|
| 启动后端 | `uvicorn backend.main:app` | 200 OK | 200 OK | ✅ |
| 健康检查 | `GET /api/health` | `{"status":"ok"}` | `{"status":"ok"}` | ✅ |
| 文字提取 | "周五下午三点在3楼会议室…" | Q3规划会 / 置信度 0.95 | Q3规划会 / 置信度 0.95 | ✅ |
| 聊天记录 | 张三李四讨论Q3目标 | 提取确认的时间 | 讨论Q3目标 / 7月20日 | ✅ |
| 模糊时间 | "下周找个时间吃个饭" | 低置信度 → pending | 团队聚餐 / 置信度 0.6 | ✅ |
| 文件上传 | 多格式文件 | 自动调度解析 | 待实机文件测试 | ⏳ |
| Demo 加载 | `POST /api/tasks/demo` | 7 条任务 | 7 条任务 | ✅ |
| 任务列表 | `GET /api/tasks` | 排序返回 | 按时间排序 | ✅ |
| 确认/拒绝 | `POST …/confirm` `…/reject` | 状态变更 | 状态变更 | ✅ |
| 前端编译 | `npx vite build` | 无 Error | 编译通过 | ✅ |
| 前端代理 | Vite → Backend | API 正常转发 | 正常 | ✅ |
| 前端首页 | http://127.0.0.1:5173 | 200 OK | 200 OK | ✅ |

---

## 2026-07-15 架构重构：本地化 + 解耦

### 核心变更

#### 1. 图片 OCR 本地化（GPT Vision → RapidOCR）

- 删除 GPT-4o Vision API 调用，改用 RapidOCR（~30MB ONNX 模型）
- 图片文字提取完全离线，零网络依赖
- 图像→纯文字的职责从"GPT 看图"改为"本地 OCR"

#### 2. 语音识别本地化（Whisper API → SenseVoice-Small）

- 删除 Whisper API 全部代码
- 集成阿里达摩院 SenseVoice-Small（227MB INT8 量化）
- 中文准确率 96%+，70ms 处理 10 秒音频，支持情感识别
- 初期考察了 X-ASR（587MB FP32），经横向对比后替换为 SenseVoice

#### 3. LLM API 去 GPT 化

- 配置变量重命名：`OPENAI_API_KEY` → `LLM_API_KEY` 等
- `.env` 仅保留 HOST/PORT，API 凭据通过 UI 设置面板配置
- 持久化到 `data/runtime_config.json`，重启不丢
- `response_format` Structured Outputs 改为普通 JSON（兼容非 OpenAI 模型）
- 支持 DeepSeek、阿里百炼、智谱等所有 OpenAI Chat API 兼容服务

#### 4. 前端重构

- **设置面板重建**：顶部展示本地模型状态（只读），中部 LLM API 配置区，底部兼容服务说明
- **Header 状态指示器**：🟢 API 已连接 / 🟡 API 未配置，未配置时显示黄色提示条
- **输入面板合并**：文字区和拖拽区同屏，一次提交，不再分 Tab
- API Key 掩码保护：保存时自动过滤 `***` 占位符

#### 5. Bug 修复

- `dispatcher._supported_extensions()` 空壳方法实现
- `task_store.confirm()` 对非 pending 任务正确返回 None
- 文件上传改用 `asyncio.to_thread` 避免同步阻塞
- PDF 解析器增加扫描件 OCR 回退

#### 6. 数据持久化

- 任务数据写入 `data/tasks.json`，重启/关机不丢失
- API 配置写入 `data/runtime_config.json`
- `.gitignore` 排除 `data/models/` 和 `data/runtime_config.json`

### 新增文件

| 文件 | 用途 |
|------|------|
| `backend/services/runtime_config.py` | LLM API 运行时配置存储 |
| `backend/routers/config.py` | GET/POST `/api/config` 端点 |
| `frontend/src/components/SettingsModal.tsx` | 设置面板 UI |

### 新增依赖

```
rapidocr-onnxruntime>=1.2.0   # 本地图片 OCR
sherpa-onnx>=1.13.0            # 本地语音识别运行时
soundfile>=0.12.0              # 音频文件读取
modelscope                      # 模型下载（dev 依赖）
```

### 模型文件

| 模型 | 大小 | 位置 |
|------|------|------|
| SenseVoice-Small INT8 | ~227MB | `data/models/sensevoice/` |
| RapidOCR | ~30MB（pip 安装） | venv 中 |

### 当前架构

```
🖼️ 图片 → RapidOCR (本地)    → 纯文字 ─┐
🎙️ 音频 → SenseVoice (本地)  → 纯文字 ─┤
📄 文档 → PyMuPDF/docx (本地) → 纯文字 ─┤
📝 文字 → 直接输入             → 纯文字 ─┘
                                        │
                                   ☁️ 用户配置的 LLM API
                                   (设置面板 ⚙️ 唯一入口)
                                        │
                                        ▼
                                     日程 JSON
```

### 时间统计

| 任务 | 预估 | 实际 |
|------|------|------|
| 4 个 Bug 修复 | 0.5h | 0.5h |
| API 配置面板（前端+后端） | 1.5h | 1h |
| 输入面板合并 | 0.5h | 0.5h |
| X-ASR 调研+集成 | 2h | 1.5h |
| SenseVoice-Small 替换 | — | 1.5h |
| RapidOCR 替换 GPT Vision | 0.5h | 0.3h |
| 去 GPT 默认值重构 | 1h | 0.5h |
| 数据持久化 | 0.5h | 0.3h |
| PDF 扫描件 OCR 回退 | 0.3h | 0.2h |
| **合计** | — | **~6.3h** |

### 高优先级（P1）

- [ ] 文件上传 + 解析的全流程端到端测试（准备测试文件：一张海报截图、一段语音、一个 docx/pdf）
- [ ] 大音频文件分段处理（当前超过 25MB 直接报错）
- [ ] API 错误时前端展示更友好的提示

### 中优先级（P2）

- [ ] 图片 OCR 实测（海报、聊天截图、通知照片）
- [ ] 音频 ASR 实测（中文语音识别准确率）
- [ ] 演示脚本准备 + 兜底离线 Demo 数据

### 低优先级（Nice to have）

- [ ] .doc 旧格式支持
- [ ] 拖拽调整任务时间
- [ ] 导出 iCalendar (.ics)
- [ ] 任务搜索/过滤
- [ ] 深色模式

---

## 时间统计

| 模块 | 预估 | 实际 | 备注 |
|------|------|------|------|
| 项目初始化 + 架构设计 | 2h | — | |
| 后端骨架 + 数据模型 | 2h | — | |
| 5 个解析器 | 1h | — | |
| LLM 提取 + Prompt 工程 | 3h | — | |
| 置信度路由 | 1h | — | |
| 前端组件 | 4h | — | |
| FullCalendar 集成 | 1h | — | |
| 集成调试 + Bug 修复 | — | 0.5h | 6 个 bug 全部修复 |
| 测试 + 验证 | — | 0.5h | 全部 API + 前端联通 |

---

## 2026-07-15 重构 + Agent 化 + 桌面应用

### 项目改名
- 正式命名为 **日知 (Rizhi)**，所有 UI/标题/文档更新

### Agent 统一意图入口
- `POST /api/tasks/intent` — 一次 LLM 调用完成意图分类 + 执行（extract/delete/chat）
- `dispatch_intent()`, `match_tasks_to_delete()` — LLM 意图路由
- 前端删除 `DELETE_KEYWORDS` 正则匹配

### 模糊时间规则集中化
- 新建 `backend/services/time_resolver.py` — 15 条规则，system prompt 自动同步
- 重复事件自动展开（LLM + Python 双重兜底）
- 中文时间表达式解析（`7月14日 16:00` + 模糊时间词）

### LLM 调用可靠性
- `timeout=60.0` + `_retry_api_call()` 3 次重试
- `ModelProfile` 自动检测 — 推理模型 max_tokens=16384
- JSON 解析兼容 markdown 包装/数组/裸对象

### 桌面应用
- `app.py` — pywebview 原生 Windows 窗口
- `launch.py` — Python 交互式菜单（解决 bat GBK 编码）
- 放弃 frameless — 原生边框更稳定

### 前端重设计
- 无滚动条布局（`h-full flex flex-col overflow-hidden`）
- 输入栏固定底部，日历 `height="100%"` 自适应
- 紧凑一行输入面板

### Bug 修复
- 所有 `Path("data/...")` → `__file__` 绝对路径（5 个文件）
- 启动时自动加载已持久化任务
- `task_store.add_all()` 去重（标题+日期+来源）
- 删除 Demo 功能（按钮/路由/文件）
- PDF 上传 → LLM 空返回（推理模型 token 不足）
- pdf 中文提取乱码（PyMuPDF 字体）→ 切换到 OCR 回退

### 新增/修改的关键文件
| 文件 | 操作 |
|------|------|
| `backend/services/time_resolver.py` | 新建 |
| `backend/services/llm_extractor.py` | 重构（+250 行） |
| `backend/routers/tasks.py` | 重构（+100 行） |
| `app.py` / `launch.py` | 新建 |
| `frontend/src/components/InputPanel.tsx` | 重构 |
| `frontend/src/components/TaskTimeline.tsx` | 重构 |
| `frontend/src/components/Layout.tsx` | 重构 |
| `CLAUDE.md` / `README.md` / `开发日志.md` | 重写 |

### 时间统计
| 任务 | 实际 |
|------|------|
| 模糊时间规则模块 | 0.5h |
| Agent 统一意图 | 1.5h |
| LLM 可靠性修复 | 2h |
| 桌面应用 | 2h |
| 前端重构 | 1h |
| Bug 修复 | 2h |
| 文档更新 | 0.3h |
| **合计** | **~9.3h** |

---

## 2026-07-15 功能开发 + Bug 修复（10:00-18:00）

### 连接问题排查与修复

#### 1. retry 逻辑 Bug — async generator 无法捕获调用方异常
**现象**：所有 LLM API 调用失败在第一次网络错误，从不重试，返回 "Connection error."。
**原因**：`_retry_api_call` 使用 async generator 的 `yield` + `try/except` 模式。Python 的 async generator 协议中，`async for` 循环体内的异常不会传播回 generator 的 `except` 块，永远只执行一次。
**修复**：重写为接受 callable 的普通 async 函数，直接 await + except。

#### 2. Windows ProactorEventLoop 与 httpx 不兼容
**现象**：pywebview daemon 线程内所有 LLM API 调用返回 "Connection error."，但 CLI 和 curl 正常。错误链：`ConnectError: All connection attempts failed`。
**原因**：Windows 默认使用 ProactorEventLoop（IOCP），httpx 的 async 连接池在 daemon 线程下与之不兼容。
**修复**：`main.py` 和 `app.py` 入口处添加 `asyncio.set_event_loop_policy(asyncio.WindowsSelectorEventLoopPolicy())`。

#### 3. 显式 httpx 客户端配置
**修复**：`_build_client()` 创建显式 `httpx.AsyncClient(proxy=None, trust_env=False, verify=certifi.where(), timeout=...)`。

### 新增模块

#### 4. 上下文提示模块 `context_hints.py`
- **65 条任务类型规则**：`(keyword, (hour, minute), category, typical_duration, kind)`
- 覆盖 12 大类：社交、用餐、工作、运动、医疗、学习、娱乐、旅行、交通、杂务、个人、财务、节日
- `lookup_task_type(text)` — 关键词匹配返回默认时间+典型时长+kind
- `build_context_prompt()` — 从数据自动生成 LLM prompt
- `extract_attendees_from_text()` — 正则提取隐含参与人（"和产品团队碰一下"→"产品团队"）
- `resolve_fuzzy_range()` — "这几天"→3天,"这两天"→2天

#### 5. 模块化语言包 `lang/`
- **321 条规则，9 个分类**：儿化音(39)、删除信号(55)、时间口语(48)、社交口语(46)、提醒信号(26)、网络用语(27)、地域变体(26)、否定拒绝(26)、确认同意(28)
- `normalize(text)` — "把事儿推了"→"把事情全部取消"，在 LLM 看到文本前标准化
- `build_prompt()` — 从数据自动生成 LLM prompt 的"Colloquial Chinese Dictionary"章节
- 数据与代码分离，加规则只需在 `data.py` 对应分类列表加一行

#### 6. 对话记忆 `conversation_memory.py`
- 保留最近 **3 轮**对话（用户输入 + 操作结果摘要）
- 注入 `dispatch_intent` 的 user prompt 中
- 解决"把这个改到明天"/"刚才那个删了"等代词指代问题

#### 7. 任务类型系统：event / deadline / milestone
- `TaskKind` enum + `category` 字段新增到 Task 模型
- `context_hints.py` 每个规则 5 元组包含 kind 字段
- 📅 event：有时间段（开会、吃饭、跑步）— 虚线框
- 🔴 deadline：单时间点（还款、DL、交房租）— 红色标记，allDay
- ⭐ milestone：年度日期（生日、纪念日、春节）— 紫色标记，allDay

#### 8. PPTX 解析器 `parsers/pptx_parser.py`
- 新增依赖 `python-pptx>=1.0.0`
- 逐页提取文本框 + 表格（markdown化）+ 演讲者备注
- 支持 Placeholder 类型标签（标题/正文/页脚）

### 前端重构

#### 9. FullCalendar 工具栏
- 从 `headerToolbar={false}` 改为启用内置工具栏
- `◀ ▶ [今天]` + `[日][周][月][列表]` 视图切换
- day/week/month/list 四个视图，自带左右箭头切换时间范围
- 自定义 indigo 配色按钮 CSS

#### 10. 任务编辑 + 撤销恢复
- `PATCH /api/tasks/{id}` — 编辑标题、时间、kind
- `POST /api/tasks/undo` — 撤销最近一次删除
- `task_store._trash_batches` — 批次垃圾箱，一次 undo 恢复整个批次
- TaskDetail 编辑模式：datetime-local 选择器，可改时间/类型/标题
- 编辑后自动确认 pending 任务

#### 11. 语音输入（本地 SenseVoice 实时流式）
- 前端 AudioContext 采集 PCM → 手动写 WAV 头 → POST `/api/transcribe-voice`
- SenseVoice-Small 本地转录，1 秒间隔实时流式显示
- 版本号机制（`voiceVersionRef`）防止异步竞态
- `typedByUser` 标记区分手动输入和语音转录

### 功能增强

#### 12. 去重修复
**原因**：`(title, date, source)` 中 source 总是不同（文件名 vs "manual-input"），去重形同虚设。
**修复**：改为 `(title, date, hour)`。

#### 13. 非任务词过滤
**修复**：`map_item_to_task` 精确匹配过滤纯标记词（"结束""开始""休息""安排""备注""注意"等）。

#### 14. 时长合理性守卫
- Python 后处理：LLM 返回 >4× 典型时长 → 裁切到典型时长
- 白名单：设计/开发/比赛/培训/旅行/搬家/考试/笔试等马拉松类 → 24× 典型时长上限
- Prompt：禁止用餐类任务（早餐/午餐/晚餐）串联到下一个事件

#### 15. 隐含结束时间推理
- Prompt 新增：从相邻事件推断前一个事件结束时间
- 极限条件：≤2 小时间隔，非用餐类

#### 16. 地点内联显示
- 任务上直接显示 📍地点名，无需点开详情

#### 17. 冲突检测
- 纯前端 `findConflicts()`：时间重叠的 event 任务 → 红色背景+脉冲闪烁

#### 18. run.bat
- 4 行批处理，双击启动

### 新增文件

| 文件 | 用途 |
|------|------|
| `backend/services/context_hints.py` | 任务类型默认值 + 与会人提取 + 模糊范围 |
| `backend/services/lang/__init__.py` | 口语标准化 + prompt 生成 |
| `backend/services/lang/data.py` | 321 条规则，9 分类 |
| `backend/services/conversation_memory.py` | 3 轮对话记忆 |
| `backend/services/parsers/pptx_parser.py` | PPTX 解析器 |
| `run.bat` | 双击启动脚本 |
| `data/trash.json` | 删除任务垃圾箱 |

### 新增依赖

```
python-pptx>=1.0.0
certifi
```

### Bug 修复统计

| Bug | 耗时 |
|-----|------|
| retry 逻辑失效 | 0.5h |
| ProactorEventLoop + httpx 不兼容 | 1.5h |
| "事儿"→"事" 儿化音口语不识别 | 0.3h |
| 午夜默认时间无意义（约会14:00→18:00） | 0.3h |
| 语音 webm 格式 soundfile 不解码 | 0.5h |
| 语音文字发送后不清空（异步竞态） | 1h |
| PPT 重复添加任务（语音转录附带） | 0.2h |
| 截止日在日历不显示（零时长事件） | 0.3h |
| 编辑任务后视图不刷新 | 0.2h |
| 去重失效（source 字段导致） | 0.1h |
| "早餐 08:30→11:30" 离谱时长 | 0.5h |
| 相接事件视图错开 | 0.5h |
| **合计** | **~6.2h** |

### 时间统计

| 任务 | 实际 |
|------|------|
| 连接问题排查与修复 | 2h |
| 上下文提示 + 语言包 + 对话记忆 | 2h |
| 任务类型系统 | 1h |
| PPTX 解析器 | 0.3h |
| 前端日历工具栏 + 编辑 + 撤销 | 1.5h |
| 语音输入（本地流式） | 1.5h |
| 功能增强 | 1h |
| Bug 修复 | 2h |
| 文档更新 | 0.5h |
| **合计** | **~11.8h** |
| **项目累计** | **~27.6h** |
